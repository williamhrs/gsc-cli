---
"@gsc-cli/sdk": patch
"@gsc-cli/cli": patch
---

Support the `hour` dimension and the `hourly_all` data state on `analytics query`. The `hour` dimension gives a 24-hour traffic view and is normally requested together with `--data-state hourly_all`.
